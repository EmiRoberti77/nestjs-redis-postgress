## Preferred: single URL (auto-detects TLS via scheme)
## - Use redis://... for NON-TLS endpoints
## - Use rediss://... for TLS-enabled endpoints
# REDIS_URL=redis://default:YOUR_PASSWORD@redis-xxxx.cloud.redislabs.com:12345
# REDIS_URL=rediss://default:YOUR_PASSWORD@redis-xxxx.cloud.redislabs.com:12345

## Alternative: host/port + credentials
REDIS_HOST=redis-17961.crce204.eu-west-2-3.ec2.cloud.redislabs.com
REDIS_PORT=17961
REDIS_USER=default
REDIS_PASS=YOUR_PASSWORD

## If you see OpenSSL "wrong version number", you enabled TLS against a non-TLS port.
## Set REDIS_TLS=false for non-TLS endpoints.
REDIS_TLS=false

## Optional: certificate validation when TLS is enabled
## - Default behavior is secure (rejectUnauthorized=true)
## - Only set this to false if you have a self-signed cert and understand the risk
# REDIS_TLS_REJECT_UNAUTHORIZED=true

PORT=3000

